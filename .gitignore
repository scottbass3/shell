# User state lives outside the checkout, in ~/.local/state/quickshell —
# nothing under it is tracked here. Only build artifacts and noise below.

# ── Build artifacts (rebuilt by install.sh) ──────────────────────────────────
/Caelestia/
/blobs-plugin/build/

# ── Editor / OS noise ────────────────────────────────────────────────────────
*.swp
*~
.DS_Store
