[general]
mode = normal
bars = 48
framerate = 60

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 100
channels = mono

[smoothing]
; Lower = snappier transients → cleaner onset detection (default 0.77)
noise_reduction = 0.20
